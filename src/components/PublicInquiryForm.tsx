"use client";
import { FormEvent, useState } from "react";
export function PublicInquiryForm() { const [message, setMessage] = useState(""); const [sending, setSending] = useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setSending(true); const f = new FormData(e.currentTarget); const r = await fetch("/api/public/inquiries", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ type: "availability", name: f.get("name"), email: f.get("email"), phone: f.get("phone"), details: { package: f.get("package"), moveDate: f.get("moveDate") } }) }); const p = await r.json().catch(() => null) as { reference?: string; error?: { message?: string } } | null; setMessage(r.ok ? `Thanks — your request is ${p?.reference}. We’ll be in touch shortly.` : p?.error?.message ?? "We couldn’t submit that request."); setSending(false); if (r.ok) e.currentTarget.reset(); } return <form className="public-form" onSubmit={submit}><input name="name" placeholder="Your name" required /><input name="email" type="email" placeholder="Email address" required /><input name="phone" placeholder="Phone number" /><select name="package" defaultValue=""><option value="" disabled>Choose a package</option><option>Quick Pack · 12 totes</option><option>Apartment · 20 totes</option><option>Home · 40 totes</option><option>Large Home · 60 totes</option></select><input name="moveDate" type="date" required /><button className="gold-button" disabled={sending}>{sending ? "Sending…" : "Check availability"}</button>{message && <p className="public-message">{message}</p>}</form>; }
